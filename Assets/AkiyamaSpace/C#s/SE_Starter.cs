using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SE_Starter : MonoBehaviour
{
    [SerializeField]
    AudioClip ThisSE;
    public bool Loop_Mode;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
